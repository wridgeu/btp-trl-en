projection;
strict ( 2 );
use draft;

define behavior for ZMRB_C_CUSTOMER alias C_Customer
{
  use create;
  use update;
  use delete;

  use action Activate;
  use action Edit;
  use action Discard;
  use action Prepare;
  use action Resume;
}